import { getMetadata } from "@/utils/helper-functions";
import React from "react";

export const metadata = getMetadata({
  title: "Information",
});

const ProfileInformation = () => {
  return <h4>Profile Information</h4>;
};

export default ProfileInformation;
